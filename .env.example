# ===========================================
# Docling App Frontend 환경 설정
# ===========================================
# 이 파일을 복사하여 .env.local 파일을 생성하세요:
# cp .env.example .env.local
#
# 프로덕션 배포 시 .env.production 파일을 수정하세요.
# ===========================================

# ===========================================
# API 설정
# ===========================================
# API Base URL (프론트엔드에서 백엔드 호출 시 사용)
#
# 환경별 설정:
# - 개발: 빈 문자열 (next.config.ts rewrites로 프록시)
# - 프로덕션: 빈 문자열 또는 실제 API 서버 URL
#
# 예시:
# - 개발: NEXT_PUBLIC_API_BASE_URL=
# - 프로덕션 (nginx 프록시): NEXT_PUBLIC_API_BASE_URL=
# - 프로덕션 (직접 호출): NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com
NEXT_PUBLIC_API_BASE_URL=

# ===========================================
# 개발 환경 전용 설정
# ===========================================
# 백엔드 프록시 URL (개발 환경에서 next.config.ts rewrites에서 사용)
# 기본값: http://localhost:8000
BACKEND_URL=http://localhost:8000
